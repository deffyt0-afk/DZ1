Гипотезы:
1. У логической регрессии точность будет нижем чем у дерева решений(нет)
2. При увелечение глубины дерева до >10 появится переобучение(да)
3. Новый компонент BMI(индекс массы тела) является основополагающим для определения степени ожирения(да)
4. Удаление одного из коррелирующих признаков(Height, Weight, BMI) не сильно ухудшит F1 marco у логической регрессии
5. FAF И BMI буду иметь отрицательную корреляцию(да)
